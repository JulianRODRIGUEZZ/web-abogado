---
const teamMembers = [
  {
    name: "Sarah Johnson",
    role: "Abogada Penalista",
    image: "/abogado.webp?height=400&width=400",
    bio: "Especialista en derecho empresarial con enfoque en fusiones y adquisiciones. Asesora a compañías durante procesos de expansión y restructuración. Tiene un doctorado en Derecho Mercantil y habla tres idiomas, lo que le permite trabajar con empresas internacionales.",
  },
  
];
---


  <section class="relative py-16 md:py-24 overflow-hidden">
    <div class="absolute -top-24 -left-24 h-96 w-96 rounded-full " />
    <div class="container mx-auto relative">
      <div class="mx-auto max-w-[105ch] text-center">
        <div class="inline-block rounded-full bg-white/5 px-3 py-1 text-sm font-medium backdrop-blur-md border border-white/10">
          Nuestros Expertos
        </div>
        <h2 class="mt-6 text-3xl font-bold tracking-tight sm:text-4xl bg-clip-text " data-aos="fade-left">
          Perfil Profesional
        </h2>
        <p class="opacity-70 mt-4  text-left  " data-aos="fade-left">
          En <strong> Osma Corporación Legal </strong> somos abogados especializados en derecho penal, derecho de familia
y sucesiones, así como en cualquier otro tipo de proceso que requiera una solución ágil, legal y
efectiva. Nos dedicamos con compromiso y ética a brindar asesoría y representación jurídica a
quienes enfrentan situaciones legales complejas, ofreciendo respuestas claras, soluciones ajustadas a
la ley y un acompañamiento cercano en cada etapa del proceso.
        </p>
      </div>
      <div class="mt-14 grid gap-8 sm:grid-cols-2 lg:grid-cols-1" data-aos="fade-left">
        {teamMembers.map((member) => (
          <div class="group flex flex-col items-center text-center">
            <div class="mb-4 overflow-hidden rounded-full border-2 border-blue-500/50 p-1">
              <div class="overflow-hidden rounded-full">
                <img
                  src={member.image}
                  alt={member.name}
                  class="h-48 w-48 object-cover transition-transform duration-300 group-hover:scale-105"
                />
              </div>
            </div>
            <h3 class="text-xl font-bold ">{member.name}</h3>
            <p class="text-sm text-black-400">{member.role}</p>
            <p class="mt-4 opacity-70 text-center max-w-[105ch] ">{member.bio}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />

  <script>
    // Importa y inicializa AOS
    import AOS from 'aos';
    
    document.addEventListener('DOMContentLoaded', () => {
      AOS.init({
        duration: 800,
        once: true,
        offset: 100
      });
    });
  </script>